/*动态计算rem*/
var _ziHao = 100 * ($(window).width() / 720);
$("html").attr("style", "font-size:" + _ziHao + "px !important");

function layermsg(text) {
	layer.open({
		content: text,
		btn: '我知道了'
	});
}